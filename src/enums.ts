export enum Sizes {
  XS = 'xs',
  S = 's',
  M = 'm',
  L = 'l',
  XL = 'xl',
}

export enum Categories {
  Jacket = 'jacket',
  Shorts = 'shorts',
}

export enum DeliveryMethods {
  PickUp = 'pick-up',
  ToDoor = 'to-door',
}

export enum PaymentMethods {
  Card = 'card',
  Cod = 'cod',
}
